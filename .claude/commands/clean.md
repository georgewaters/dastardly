---
description: Clean build artifacts
---

Clean all build artifacts from the monorepo:
1. Remove all `dist/` directories in packages
2. Remove all `node_modules/` directories
3. Show what was cleaned
